(function (w) {
  const E = (w.__ENV__ = w.__ENV__ || {});

  const API_BASE = "${PLATFORM_API_BASE}";
  if (API_BASE) E.NEXT_PUBLIC_API_BASE = API_BASE;
  const SSO_URL = "${PLATFORM_SSO_URL}";
  if (SSO_URL) E.NEXT_PUBLIC_SSO_URL = SSO_URL;
  const REALM = "${PLATFORM_SSO_REALM}";
  if (REALM) E.NEXT_PUBLIC_SSO_REALM = REALM;
  const CID = "${PLATFORM_SSO_CLIENT_ID}";
  if (CID) E.NEXT_PUBLIC_SSO_CLIENT_ID = CID;
  const CLARITY = "${PLATFORM_CLARITY_KEY}";
  if (CLARITY) E.NEXT_PUBLIC_CLARITY_KEY = CLARITY;
  const PIWIK = "${PLATFORM_PIWIK_KEY}";
  if (PIWIK) E.NEXT_PUBLIC_PIWIK_KEY = PIWIK;
  const FORM = "${PLATFORM_FORM_SPREE}";
  if (FORM) E.NEXT_PUBLIC_FORM_SPREE = FORM;
  const BASEURL = "${PLATFORM_LOCAL_BASE_URL}";
  if (BASEURL) E.NEXT_PUBLIC_LOCAL_BASE_URL = BASEURL;
  const REDIRECT = "${PLATFORM_LANDING_REDIRECT_URL}";
  if (REDIRECT) E.NEXT_PUBLIC_REDIRECT_URL = REDIRECT;
})(window);
